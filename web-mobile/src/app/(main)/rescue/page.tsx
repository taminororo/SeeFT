"use client";

import { StatusMessage } from "@/components/ui/status-message";

// Phase 3-4 で実装予定（緊急時対応）。
export default function RescuePage() {
  return <StatusMessage>緊急時対応（準備中）</StatusMessage>;
}
